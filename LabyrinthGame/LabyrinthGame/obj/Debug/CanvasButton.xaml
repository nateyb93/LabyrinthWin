<UserControl
    x:Class="LabyrinthGame.CanvasButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LabyrinthGame"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    
    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <Canvas HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Name="canvas" Margin="10"/>
        <Button x:ConnectionId='1' HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Name="button"                     Margin="2"/>
        <Grid Name="ConfirmationButtons" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Button x:ConnectionId='2' Name="YesButton" Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Content="Yes" Foreground="Lime"                          />
            <Button x:ConnectionId='3' Name="NoButton" Grid.Row="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Content ="No" Foreground="Red"                          />
        </Grid>
    </Grid>
    
</UserControl>

