<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LabyrinthGame"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Controls="using:Callisto.Controls"
    x:Class="LabyrinthGame.MainMenuPage"
    mc:Ignorable="d">
    <Page.Resources>
        <Style TargetType="Button" x:Name="MenuButton">
            <Setter Property="Width" Value="400"/>
            <Setter Property="Height" Value="100"/>
            <Setter Property="FontSize" Value="36"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="Button" x:Name="FlyoutButton">
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="FontSize" Value="24"/>
        </Style>
    </Page.Resources>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="{StaticResource AppTitle}" FontSize="120" HorizontalAlignment="Center" Margin="0,0,0,50"/>
            <Button x:ConnectionId='1' x:Name="PlayButton" Content="{StaticResource PlayButtonText}"                          Background="Lime" Foreground="Black" Style="{StaticResource MenuButton}"/>
            <Button x:ConnectionId='2' x:Name="AboutButton" Content="{StaticResource AboutButtonText}"                           Background="Blue" Style="{StaticResource MenuButton}"/>
            <Button x:ConnectionId='3' x:Name="HowToButton" Content="{StaticResource HowToPlayButtonText}"                           Background="Yellow" Foreground="Black" Style="{StaticResource MenuButton}"/>
        </StackPanel>
    </Grid>
</Page>

